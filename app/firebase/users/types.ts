export interface UserDoc {
  id: string;
  username: string;
  code: string;
  displayName: string;
}

export interface User {
  id: string;
  username: string;
  code: string;
  displayName: string;
}
